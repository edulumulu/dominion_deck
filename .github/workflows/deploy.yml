name: Deploy

# Deploy desactivado — la app no está desplegada en ningún entorno.
# Para reactivar, descomentar el trigger y añadir los secrets de Fly.io.

# on:
#   workflow_run:
#     workflows: ["CI"]
#     types: [completed]
#     branches: [main]
